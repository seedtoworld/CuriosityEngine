[project]
name = "Curiosity Engine"
version = "0.1.0"
description = "Autonomous curiosity-driven web exploration engine"
authors = [{name = "AN32"}]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"